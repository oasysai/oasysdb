[package]
name = "oasysdb"
version = "0.7.0"
edition = "2021"
license = "Apache-2.0"
readme = "readme.md"

# Information.
authors = ["Edwin Kys", "Oasys"]
description = "Fast & scalable vector store with ground-up hybrid ANN search support."
homepage = "https://docs.oasysdb.com"
repository = "https://github.com/oasysai/oasysdb"

# Metadata.
keywords = ["vector", "database", "anns", "search", "simd"]
categories = ["database", "algorithms", "data-structures"]

[dependencies]
uuid = { version = "1.9.1", features = ["v4", "fast-rng", "serde"] }
half = { version = "2.4.1", features = ["serde"] }
url = "2.5.2"
futures = "0.3.30"
rand = "0.8.5"

# Parallelism.
simsimd = { version = "4.4.0", optional = true }
rayon = "1.10.0"

# Serialization.
serde = { version = "1.0.203", features = ["derive"] }
bincode = "1.3.3"
serde_json = "1.0.120"

[dependencies.sqlx]
version = "0.7.4"
default-features = false
features = ["all-databases"]

[dev-dependencies]
byteorder = "1.5.0"
curl = "0.4.44"
flate2 = "1.0.30"
tar = "0.4.41"

[features]
simd = ["dep:simsimd"]

[profile.release]
lto = true
opt-level = "z"
codegen-units = 1
