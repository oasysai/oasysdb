syntax = "proto3";
package database;

import "google/protobuf/empty.proto";

service Database {
    rpc Heartbeat(google.protobuf.Empty) returns (google.protobuf.Empty);
}

// Shared Types
enum Metric {
    COSINE = 0;
    EUCLIDEAN = 1;
}
